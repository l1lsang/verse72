export type TestType = "DUNAMIS" | "YEDADAM";

/* =========================
   🧠 시험 문제
   ========================= */
export type TestQuestion = {
  id: string;
  mode: TestType;

  type:
    | "WORD_BLANK"        // 두나미스: 단어 빈칸 (복수)
    | "TWO_PHRASE_REST"   // 두나미스: 앞 두 어절
    | "YEDADAM_HARDCORE"; // 예닮공: 하드코어

  /* =========================
     ❓ 문제 텍스트
     ========================= */
  prompt: string;

  /* =========================
     ✍ 사용자 입력값 (UI)
     ========================= */
  input?: {
    chapter?: string; // 예닮공: "? 장"
    verse?: string;   // 예닮공: "? 절 (6,7 / 37~39)"
    text?: string;    // 🔥 입력한 말씀 전체 or 빈칸 답
    texts?: string[]; // ✅ WORD_BLANK: 빈칸 여러 개 입력
  };

  /* =========================
     🧮 채점용 정답 (UI 비노출)
     ========================= */
  answers: {
    chapter?: number; // 예닮공 전용
    verse?: string;   // 예닮공 전용

    // ✅ 공통: 단일 / 다중 모두 허용
    text?: string;     // TWO_PHRASE_REST / YEDADAM
    texts?: string[];  // WORD_BLANK (4~5개 단어)
  };

  /* =========================
     📖 원본 말씀
     ========================= */
  verse: VerseData;
};

/* =========================
   📜 말씀 데이터
   ========================= */
export type VerseData = {
  id: string;

  /* =========================
     📖 메타데이터
     ========================= */
  book: string;   // 예닮공에서도 책 이름은 항상 노출
  group: string;  // A~F
  theme: string;  // 주제

  /* =========================
     🔢 장 / 절
     ========================= */
  chapter: number; // 단일 장
  verse: string;   // "6,7" | "37~39"

  /* =========================
     📜 말씀 본문
     ========================= */
  text: string;
};
